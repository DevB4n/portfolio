<div id="Map-Background">
  <!-- Back Button -->
  <a href="/" class="back-btn" title="Volver al inicio">
    <span class="back-icon">◀</span> back
  </a>

  <div class="map-wrapper">
    <img src="/my_travel/mapa_tesoro_islas(1).png" alt="Treasure Map" />
    
    <!-- Island Links - Adjust the top, left, width, and height percentages to perfectly cover each island in the image -->
    <a href="/Frontend_island" class="island-link frontend" title="Frontend Island"> 
    </a>
    
    <a href="/Backend_island" class="island-link backend" title="Backend Island">
    </a>
    
    <a href="/Security_island" class="island-link security" title="Security Island">
    </a>
    
    <a href="/Soft_Skills_island" class="island-link soft-skills" title="Soft Skills Island">
    </a>
  </div>
  
  <!-- Tip Modal -->
  <div class="treasure-tip">
    <span class="tip-icon">💡</span>
    <p><strong>Tip:</strong> Maybe click on some island could guide you to find the hidden treasure.</p>
  </div>
</div>

<style>
    body {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
    }
    
    #Map-Background {
        position: relative;
        width: 100vw;
        height: 100vh;
        display: flex;
        justify-content: center;
        align-items: center;
        background-color: rgba(151, 118, 68, 0.7);
        backdrop-filter: blur(8px);
        overflow: hidden;
    }
    
    /* This wrapper perfectly matches the 1536x1024 aspect ratio of the image without overflowing the screen */
    .map-wrapper {
        position: relative;
        width: 100%;
        height: 100%;
        max-width: calc(100vh * (1536 / 1024));
        max-height: calc(100vw * (1024 / 1536));
    }
    
    .map-wrapper img {
        width: 100%;
        height: 100%;
        object-fit: contain;
        display: block;
        pointer-events: none; /* Prevents dragging the image */
    }

    /* Common style for the clickable areas */
    .island-link {
        position: absolute;
        display: flex;
        justify-content: center;
        align-items: center;
        text-decoration: none;
        border-radius: 50%; 
        transition: transform 0.3s ease, background-color 0.3s ease;
        /* 
        */
        background-color:transparent;
    }

    .island-link:hover {
        background-color: rgba(255, 255, 255, 0.18);
    }

    /* islands link position and size */
    .frontend {
        top: 8%;
        left: 14%;
        width: 26%;
        height: 32%;
    }

    .backend {
        top: 7%;
        left: 59%;
        width: 26%;
        height: 34%;
    }

    .security {
        top: 60%;
        left: 22%;
        width: 20%;
        height: 28%;
    }

    .soft-skills {
        top: 58%;
        left: 58%;
        width: 24%;
        height: 34%;
    }

    .island-link span {
        pointer-events: none; /* Ensures the span doesn't block clicks */
    }

    /* Tip Modal Styles */
    .treasure-tip {
        position: absolute;
        bottom: 30px;
        right: -350px; /* Start off-screen */
        width: 280px;
        background: rgba(40, 30, 20, 0.85); /* Dark parchment-like backdrop */
        backdrop-filter: blur(10px);
        border: 1px solid rgba(212, 175, 55, 0.4); /* Subtle gold border */
        border-radius: 12px;
        padding: 16px 20px;
        color: #f4e4bc;
        font-family: 'Georgia', serif;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.5), inset 0 0 10px rgba(212, 175, 55, 0.1);
        display: flex;
        gap: 15px;
        align-items: center;
        z-index: 100;
        animation: slide-in-out 8s cubic-bezier(0.2, 0.8, 0.2, 1) forwards;
        animation-delay: 1.5s; /* Wait a little bit after page load */
    }

    .treasure-tip .tip-icon {
        font-size: 24px;
        filter: drop-shadow(0 0 8px rgba(255, 215, 0, 0.8));
    }

    .treasure-tip p {
        margin: 0;
        font-size: 14px;
        line-height: 1.5;
        letter-spacing: 0.5px;
    }

    .treasure-tip strong {
        color: #d4af37; /* Gold color */
        letter-spacing: 1px;
    }

    @keyframes slide-in-out {
        0% {
            right: -350px;
            opacity: 0;
        }
        10% {
            right: 30px;
            opacity: 1;
        }
        85% {
            right: 30px;
            opacity: 1;
        }
        100% {
            right: -350px;
            opacity: 0;
            pointer-events: none;
        }
    }

    /* Back Button Styles */
    .back-btn {
        position: absolute;
        top: 30px;
        left: 30px;
        z-index: 100;
        display: flex;
        align-items: center;
        gap: 8px;
        padding: 10px 20px;
        background: rgba(40, 30, 20, 0.75);
        backdrop-filter: blur(8px);
        border: 1px solid rgba(212, 175, 55, 0.5);
        border-radius: 30px;
        color: #f4e4bc;
        text-decoration: none;
        font-family: 'Georgia', serif;
        font-size: 15px;
        letter-spacing: 1px;
        transition: all 0.3s ease;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
    }

    .back-btn:hover {
        background: rgba(40, 30, 20, 0.95);
        border-color: rgba(212, 175, 55, 0.9);
        transform: translateX(-5px);
        box-shadow: 0 6px 20px rgba(0, 0, 0, 0.6);
        color: #fff;
    }

    .back-btn .back-icon {
        font-size: 22px;
        color: #d4af37;
        margin-top: -2px;
    }
</style>